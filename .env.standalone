# Docker Reverse Proxy Standalone Configuration
# This configuration is for running outside of Docker

# Docker hosts configuration
DOCKER_HOSTS=vm-switchboard.snadboy.com
SSH_USER=revp

# SSH key path - adjust to your actual SSH key location
SSH_PRIVATE_KEY_PATH=/home/revp/.ssh/id_revp

# Caddy configuration - pointing to the actual Caddy instance
CADDY_API_URL=http://vm-switchboard.snadboy.com:2019

# Logging configuration - use local directory
LOG_LEVEL=DEBUG
LOG_FILE_PATH=./logs/monitor.log
LOG_MAX_SIZE=10
LOG_BACKUP_COUNT=5

# Monitoring configuration
RECONCILE_INTERVAL=300

# API configuration
API_BIND=0.0.0.0:8080