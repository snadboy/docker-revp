# Docker Reverse Proxy Configuration
# Copy this file to .env and customize the values

# Required: Space-separated list of Docker hosts to monitor
# Format: hostname or hostname:port
DOCKER_HOSTS=localhost server1 server2:2222

# Required: SSH username for connecting to Docker hosts
SSH_USER=your-ssh-username

# SSH private key is mounted as a file (see ssh-keys/docker_monitor_key)
# Create the ssh-keys directory and copy your private key there:
# mkdir -p ssh-keys
# cp ~/.ssh/your_private_key ssh-keys/docker_monitor_key
# chmod 600 ssh-keys/docker_monitor_key

# Optional: Caddy Admin API URL
CADDY_API_URL=http://caddy:2019

# Optional: How often to reconcile container state (seconds)
RECONCILE_INTERVAL=300

# Optional: Logging configuration
LOG_LEVEL=INFO
LOG_MAX_SIZE=10
LOG_BACKUP_COUNT=5

# Optional: API server configuration
API_PORT=8080
API_HOST=0.0.0.0